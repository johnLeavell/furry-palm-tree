import React from 'react'
import ListArticles from './ListArticles';

const SearchResultsList = (articles) => {
    
    console.log(articles)
    return (
        <div>
            <ListArticles/>
        </div>
    )
}

export default SearchResultsList;